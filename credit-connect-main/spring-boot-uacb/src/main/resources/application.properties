# Application
spring.application.name=UACB - Unified Academic Credit Bank

# Server Port (Render compatible)
server.port=${PORT:8080}


# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d6dvn3kr85hc73c5s1dg-a:5432/credit_connect_db?sslmode=require
spring.datasource.username=credit_connect_db_user
spring.datasource.password=TkoXdSvv32LFwoggs2mNUzlJXLqB5pxF
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Security
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=${SECURE_COOKIE:false}

# Logging
logging.level.root=INFO
logging.level.com.uacb=DEBUG
logging.level.org.springframework.security=INFO

# Production Profile Settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# File Upload (if needed later)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
